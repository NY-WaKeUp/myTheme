# 2026-06-19 · 推送到 GitHub 仓库

## 任务

将 `myTheme`（Mountain Fork Color Theme）项目推送到 GitHub 账号 `NY-WaKeUp` 的 repositories。

## 执行步骤

1. **检查环境**
   - 本地目录 `/Users/ningyu/myTheme` 原先未初始化 git
   - `gh auth status` 已登录账号 `NY-WaKeUp`，协议为 SSH

2. **新增 `.gitignore`**
   - 排除：`.DS_Store`、`node_modules/`、`*.vsix`、误生成的 `npx` 与 `mountain-fork-color-theme@*` 文件

3. **初始化并提交**
   - `git init`
   - 首次提交 7 个文件：`.gitignore`、`.vscodeignore`、`CHANGELOG.md`、`LICENSE`、`README.md`、`package.json`、`themes/mountain-fork-color-theme.json`
   - commit: `2a2154f` — *Initial commit: Mountain Fork VS Code/Cursor color theme.*

4. **创建远程仓库并推送**
   ```bash
   gh repo create mountain-fork-color-theme \
     --public \
     --source=. \
     --remote=origin \
     --push \
     --description "Mountain - Fork dark color theme for VS Code and Cursor."
   ```

## 结果

| 项 | 值 |
|---|---|
| 仓库地址 | https://github.com/NY-WaKeUp/mountain-fork-color-theme |
| 可见性 | public |
| 默认分支 | main |
| 远程 | `origin` → `github.com:NY-WaKeUp/mountain-fork-color-theme.git` |

## 未纳入版本控制的本地文件

- `mountain-fork-color-theme-0.0.1.vsix`（构建产物，已在 `.gitignore`）
- 误生成的空文件 `npx`、`mountain-fork-color-theme@0.0.1`
